---
import Alert from "../../components/Alert.astro";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { withBase } from "../../lib/paths";
---

<BaseLayout
  title="使用指南"
  description="在 Discord 提問、使用隱密支援、管理隱私與追蹤案件的完整指南。"
  currentPath="guide"
>
  <div
    class="shell guide-shell stack section"
    style="--stack-gap: var(--space-7)"
  >
    <header class="guide-intro">
      <p><a href={withBase("")}>首頁</a> / 使用指南</p>
      <h1>在 Discord 提問</h1>
      <p>
        公開問題直接在 Discord 發文；需要隱密協助時，由 Course Manager
        開啟專屬空間。兩者的處理流程相同，只有可見範圍不同。
      </p>
    </header>

    <Alert title="正式課務以各班 NTU COOL 為準" tone="danger">
      <p>教材、作業、成績、期限、公告與課程政策，以各班 NTU COOL 為準。</p>
    </Alert>

    <section aria-labelledby="public-title">
      <h2 id="public-title">公開提問流程</h2>
      <ol class="step-list">
        <li><strong>選擇討論區</strong><br />依問題內容進入合適的區域。</li>
        <li>
          <strong>貼上問題與圖片</strong><br
          />先把你卡住的位置、已嘗試的方法與希望釐清的部分說清楚。
        </li>
        <li>
          <strong>完成案件設定</strong><br />Bot
          會詢問題型、關鍵字，以及是否允許這一案用於 AI 輔助教學分析。
        </li>
        <li>
          <strong>收到案號與連結</strong><br />Bot 整理標題後，會用 Discord
          私訊傳送案號與直達連結。
        </li>
      </ol>
    </section>

    <section aria-labelledby="areas-title">
      <div class="section-heading">
        <div>
          <h2 id="areas-title">選擇提問區域</h2>
        </div>
      </div>
      <div class="guide-cards">
        <article>
          <h3>數學問題</h3>
          <p>微積分觀念、計算與解題方法。</p>
        </article>
        <article>
          <h3>課務與系統</h3>
          <p>WeBWorK、Gradescope、教材勘誤與課程系統。</p>
        </article>
        <article>
          <h3>延伸討論</h3>
          <p>趣味題、課外延伸與值得保留的討論。</p>
        </article>
      </div>
    </section>

    <section class="private-guide" aria-labelledby="private-title">
      <div>
        <h2 id="private-title">隱密支援適用情況</h2>
        <p>
          在 Discord 指令選單建立隱密支援後，Course Manager
          會開啟只有你與授權教學團隊可見的空間。接著照一般提問流程貼出問題、選擇類型並設定
          AI 同意。
        </p>
      </div>
      <ul>
        <li>內容與圖片留在受限 Discord 空間，不經 Portal 上傳。</li>
        <li>案件同樣會收到以 <code>-P</code> 結尾的案號與直達連結。</li>
        <li>網頁查詢只顯示狀態，不顯示題目、對話、作者或附件。</li>
      </ul>
    </section>

    <section class="privacy-grid" aria-labelledby="privacy-title">
      <div class="section-heading privacy-heading">
        <div>
          <h2 id="privacy-title">隱私與 AI 同意</h2>
        </div>
      </div>
      <article>
        <h3>誰看得到</h3>
        <p>
          公開討論區由伺服器成員依權限閱讀；隱密支援只開給案件建立者與授權教學團隊。
        </p>
      </article>
      <article>
        <h3>帳號怎麼顯示</h3>
        <p>
          伺服器暱稱不會隱藏 Discord
          的全域使用者名稱、顯示名稱、頭像或個人資料。
        </p>
      </article>
      <article>
        <h3>是否允許 AI 分析</h3>
        <p>每一案分開選擇「允許／不允許」；不允許仍會得到相同的教學支援。</p>
      </article>
    </section>

    <section class="guide-prose" aria-labelledby="dm-title">
      <h2 id="dm-title">Discord 私訊與帳號安全</h2>
      <p>
        Course Manager
        會以私訊傳送申請結果、案號與直達連結。若不希望其他伺服器成員私訊，可在
        Discord 的隱私設定關閉「允許來自同一伺服器成員的私人訊息」。Bot
        能否私訊仍受個人設定影響。
      </p>
      <p>
        教學語音空間不錄音，也不自動轉錄。請勿在公開頻道張貼他人的個資、成績或私人對話。
      </p>
    </section>

    <section class="faq" aria-labelledby="faq-title">
      <h2 id="faq-title">常見問題</h2>
      <details>
        <summary>沒有收到 Bot 私訊</summary>
        <p>
          先確認私訊設定，再回到原討論串查看 Bot
          是否已完成建案；不要重複建立同一個問題。
        </p>
      </details>
      <details>
        <summary>我已經送過加入申請</summary>
        <p>
          Course Manager
          會回覆「你已經註冊過了呦！」並附上目前班級與權限，不會再建立第二筆。
        </p>
      </details>
      <details>
        <summary>案件已結案，但我還有問題</summary>
        <p>
          使用原討論串的「重新開啟並繼續提問」。案件會沿用同一個討論串與案號。
        </p>
      </details>
      <details>
        <summary>案件狀態和 Discord 畫面暫時不同</summary>
        <p>
          Discord 偶爾會延後更新標題或封存狀態。先保留原案號，稍後從 Bot
          私訊的連結回去查看。
        </p>
      </details>
    </section>
  </div>
</BaseLayout>

<style>
  .guide-shell {
    width: min(calc(100% - 2 * var(--space-4)), 72rem);
  }

  .guide-intro,
  .guide-prose {
    max-width: 60rem;
  }

  .guide-intro > p:last-child {
    font-size: clamp(1.15rem, 1.4vw, 1.35rem);
    text-wrap: pretty;
  }

  .guide-cards,
  .privacy-grid {
    display: grid;
    gap: var(--space-4);
  }

  .guide-cards article,
  .privacy-grid article {
    padding: var(--space-4);
    background: var(--color-surface);
  }

  .guide-cards {
    border-block: var(--border-thin) solid var(--color-border-strong);
  }

  .guide-cards article + article {
    border-top: var(--border-thin) solid var(--color-border);
  }

  .guide-cards :is(h3, p:last-child),
  .privacy-grid :is(h3, p:last-child) {
    margin-bottom: 0;
  }

  .privacy-grid {
    border-block: var(--border-thin) solid var(--color-border-strong);
  }

  .privacy-grid article {
    padding-block: var(--space-5);
  }

  .privacy-grid article + article {
    border-top: var(--border-thin) solid var(--color-border);
  }

  .private-guide {
    display: grid;
    gap: var(--space-4);
    padding: var(--space-5);
    border: var(--border-thin) solid var(--color-accent);
    border-inline-start-width: 0.45rem;
    border-radius: var(--radius-lg);
    background: var(--color-accent-soft);
  }

  .private-guide ul {
    margin-bottom: 0;
  }

  .privacy-heading {
    grid-column: 1 / -1;
  }

  .guide-prose p {
    text-wrap: pretty;
  }

  .faq details {
    padding-block: var(--space-3);
    border-bottom: var(--border-thin) solid var(--color-border);
  }

  .faq summary {
    cursor: pointer;
    font-weight: 750;
  }

  .faq details p {
    padding-top: var(--space-3);
  }

  @media (min-width: 48rem) {
    .guide-cards,
    .privacy-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .guide-cards article + article,
    .privacy-grid article + article {
      border-top: 0;
      border-inline-start: var(--border-thin) solid var(--color-border);
    }

    .private-guide {
      grid-template-columns: minmax(0, 1.25fr) minmax(18rem, 0.75fr);
    }
  }
</style>
