# Documentation only: runtime code reads Apps Script PropertiesService, not .env.
APP_ENVIRONMENT=fixture
FIXTURE_MODE=true
SPREADSHEET_ID=

# External project state stays blank and must never be committed with real values.
GAS_SCRIPT_ID=
GAS_DEPLOYMENT_ID=
