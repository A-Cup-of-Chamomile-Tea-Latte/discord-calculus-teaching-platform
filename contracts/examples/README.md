# Contract examples

`valid/` 為 11 種 record 的最小有效範例，`invalid/` 為必須拒絕的安全／相容性案例。`manifest.json` 指定 schema、instance 與每個無效案例的預期原因。

這些範例完全虛構，只示範契約形狀；Task 08 的跨元件情境資料另放 `fixtures/`。新增或修改 schema 時，必須同步新增有效範例及至少一個能說明新限制的無效範例。
