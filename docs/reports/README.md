# Codex reports

Every task and batch must leave a report here. Reports are part of the project record and should be committed, except when they contain local paths or diagnostics that the user chooses to keep private.

Never include secrets, token values, email verification codes, real student data, or credential file contents.
