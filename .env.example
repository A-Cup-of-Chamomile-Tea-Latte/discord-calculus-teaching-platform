# This repository does not automatically load a root .env file.
# Keep component-specific settings in their own runtime/environment store.
# See docs/CONFIGURATION.md before creating any local secret-bearing file.
